/// @description Init Vars
go = false; //whether the ball is moving
spd = 3; // base speed of the ball
dir = random_range(45, 135); // initial direction the ball is traveling
penatrate = false; // flag that represents if penatrate powerup is active
